#include <bits/stdc++.h>
#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define INF 1e9
using namespace std;
#define DEBUG(x)cout<<x<<endl;
int n,m;
int a[1000005];

int main(){
	cin>>n>>m;
	for(int i=0;i<n;i++)cin>a[i];
	std::sort(a,a+n);
}